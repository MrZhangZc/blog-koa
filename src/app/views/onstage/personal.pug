extends ../layout

append css
  link(rel="stylesheet", href="/css/personal.css")
block main
  .personal-bg
    h1 个人中心
  .container
    .row
      .persional
        img(src= tuser.headimg, alt="")
        p #{tuser.nickname}
        p #{tuser.sex}
        p #{tuser.autograph}
        p #{tuser.email}
        p #{tuser.autograph}
        P #{moment(tuser.meta.createdAt).format('YYYY-MM-DD')}
        //- form(action="/load", method="post")
        //-   input.filename(type="text" name='filename')
        //-   input.imgPath(type="file" name="file")
        //-   button.btn.btn-lg.btn-primary.btn-block(type="submit") 登录