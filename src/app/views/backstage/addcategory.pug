extends ../layout

append css
  link(rel="stylesheet", href="/css/back/add.css")
block main
  h2.page.header 添加文章分类
  form.vertical-form(method="post", action='/article/category/add')
    label(for="name") 标题
    input.form-control.input-lg(name="name", required='true',placeholder="请输入标题。。。")
    button(type="submit")= "发布"